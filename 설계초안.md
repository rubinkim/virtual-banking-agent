# 설계 초안 — 가상 금융 업무 AI Agent (LangGraph 미니 프로젝트)

> **이 문서는 무엇인가**: 내가 이 프로젝트의 설계 초안을 어떻게 만들어 왔는지를 `mini_project_issues_talking.md`(Claude와의 대화 기록)에서 간추린 것이다.
>
> **작성 원칙**
> - **"내가 직접 쓴 초안"은 원문 그대로** 인용한다(오타 포함, 수정 없음).
> - **AI 피드백**과 **내가 내린 결정**은 분리해서 적는다.
> - AI가 한 일(피드백, 구현)을 숨기지 않는다. 6장에 정직하게 정리했다.
> - 초안이 구현 과정에서 어떻게 바뀌었는지는 **"최종 설계 자료"** 에서 다룬다. 이 문서에는 요약만 둔다.
>
> 근거 기록: `mini_project_issues_talking.md`의 대화 4, 7, 12, 15, 16 (해당 절 번호를 각 장에 표기)

---

## 0. 한눈에 보기

| 업무 | 초안 작성 | AI의 역할 | 내가 내린 핵심 결정 | 상태 |
|---|---|---|---|---|
| 잔액 조회 | 초안 3종(원문 보존) | 피드백, 코드 작성 | 직접 State/Node/Edge, supervisor → account 서브그래프, 재작업 루프 제거 | 구현·검증 완료 |
| 거래내역 조회 | 생각·조건 목록(원문 보존) | 결정 지점 제시, 코드 작성 | 정해진 파라미터 방식, 기간은 Python 계산, 건수·합계 Python 계산 | 구현·검증 완료 |
| 내 계좌 간 이체 | 초안 3종(원문 보존) | 결정 지점 제시, 피드백, 코드 작성 | 노드로 쪼개 State로 연결, 별도 `transfer_agent`, 필드 정리 | 구현·검증 완료 |
| 승인 흐름 (승인 전 수정, 자연어 승인·취소) | 초안 1종(AI 제안 반영, 일부 미완성) | 결정 지점 제시, **초안이 못 다룬 부분의 설계와 구현을 위임받아 수행** | 계좌도 수정 대상, 응답 해석은 LLM, 코드가 지킬 두 규칙 | 구현·검증 완료 |
| 카드 / 청구서 / 그 밖의 이체(조건부·분할) / 별명 변경 | 없음 | - | - | **미착수** |

---

## 1. 설계 접근 (내가 정한 원칙)

1. **전체를 한 번에 설계하지 않고 업무 단위로 나눠 하나씩 설계·구현**한다. 가장 단순한 조회(잔액)로 뼈대를 세우고 그 위에 쌓는다.
2. **선생님께 배운 개념만** 쓴다. (`ToolRuntime` 같은 배우지 않은 API는 제외)
3. **`create_agent()` 같은 고수준 헬퍼 대신 State, Node, Edge를 직접 구현**하며 LangGraph를 익힌다.
4. **Supervisor 패턴**: 요청을 분석해 담당 agent로 보낸다. 부모 그래프와 서브그래프는 같은 State(`BankState`)를 공유하고, checkpointer는 부모에만 둔다.
5. **역할 분담 원칙(진행하면서 굳어진 것)**: 자연어를 해석하는 일은 LLM이, 계산·필터·정렬·검증·데이터 변경은 코드가 한다.
6. 단계마다 Git 커밋으로 남긴다(선생님 안내).

---

## 2. 잔액 조회 (대화 4, 7)

### 2.1 나의 첫 초안 (원문)

> 1. 사용자가 현재 내 잔액이 얼마야?라고 질문한다
> 2. 사용자 본인이 맞는지 인증한다. 만약 틀리다면 틀려서 잔액조회불가통보한다.
> 3. 만약 맞다면 llm이 initial_data.json에서 이 사용자의 account가 있는지 확인한다.
> 4.  만약 account가 없다면 없으니 잔액조회가 불가능하다고 통보해주고, 하나면 그 account의 잔액을 말해주고 둘이상이면 사용자에게 어떤 account인지 물어보고 나서 잔액조회를 해준다.

**AI 피드백 → 내 결정**: "인증"은 대화 중에 받는 게 아니라 **로그인했다고 가정하고 `owner_id`를 시작 시점에 넘기는 것**이라는 강의 내용이 정리됨. 인증 단계를 없애고 `owner_id`를 State의 기본 필드로 두기로 함. `initial_data.json`(원본)이 아니라 작업용 복사본을 쓰고, LLM이 파일을 직접 읽는 게 아니라 **tool(Python 함수)** 을 호출하게 하기로 함.

### 2.2 나의 두 번째 초안 (원문)

> ### 은행 계좌 잔액 조회 업무 흐름
> 1. 사용자가 은행 계좌 잔액을 조회하는 요청이 온다.
> 2. 사용자 요청이 어떤 서비스를 요구하는지를 분석해서 그 작업에 맞는 subagent에게 작업을 분배하는 역할을 하는 supervisor 노드가 이 작업을 bank_account_balance라는 subagent에게 보낸다.
> 3. 만약 사용자 요청안에 잔액 조회를 원하는 account_id가 있는지 확인해서 있다면 tool인 get_balance_by_owner(ower_id, account_id)를 갖고 있는 graph(=create_agent(model, llm, checkpointer)를 invoke해서 구하고 만약 account_id가 없다면 graph가 해당 owner_id는 몇개의 account를 갖고 있는지 확인해서 그 정보를 Human-in-the-loop의 Command, interrupt를 이용해서 사용자로부터 선택을 받아서 거기에 맞게 정보를 제공한다.)

**AI 피드백 → 내 결정**
- subagent를 "잔액 조회 하나"로 좁게 잡지 말고 계좌 **도메인**(`account_agent`)으로 넓게 잡기 → 채택.
- `account_id`가 주어져도 tool이 `owner_id`와 `account_id`의 **소유 관계를 검증**해야 함 → 채택.
- 계좌 개수(0/1/2개 이상)를 분기로 명시하기 → 채택.

### 2.3 나의 세 번째 초안 (원문, 직접 State/Node/Edge로 구현하기로 한 뒤)

> * 고객의 계좌 잔액 조회 요청 --> supervisor_agent가 사용자의 요청을 분석한다.
> * 요청 내용과 필요한 context를 가장 적당한 subagent에게 전달한다. 지금의 작업은 account_agent가 담당
> * supervisor_agent는 account_agent가 반환한 balance의 내용을 확인하고 이상이 있으면 추가작업 요청
> * 이상이 없으면 사용자에게 balance 결과를 전달한다.

**AI 피드백 → 내가 확정한 결정 (대화 4)**
1. State에 `messages`, `owner_id`를 둔다.
2. **재작업 요청 루프는 없앤다.** 잔액 조회는 결정론적 조회라 "이상이 있다"는 의미가 불분명하다는 지적에 동의.
3. 계좌가 2개 이상일 때만 `interrupt`로 사용자에게 선택받는다(→ 이 결정은 아래 2.4에서 바뀜).
4. 잔액 조회를 **서브그래프**로 만들어 부모 그래프에 `add_node`로 넣고, 부모와 **같은 State를 공유**한다.
5. checkpointer는 **부모 그래프를 compile할 때만** 설정한다.

### 2.4 설계가 크게 바뀐 지점 — "여러 계좌 = interrupt로 선택"은 틀렸다 (대화 7)

내가 던진 질문이 계기였다.

> 만약에 사용자가 계좌를 2개 갖고 있는데 이 둘다 잔액 조회를 원하는 경우 … 잔액 조회를 두개 계좌이상 원하는 경우도 있을 수 있을 거 같은데...

이 질문과 후속 확인으로 다음이 드러났다.
- "결과가 2개 이상 = 애매하다 = 되묻기"라는 규칙은 **"사용자가 여러 개를 명시적으로 요청한 경우"** 와 **"정말 특정이 안 되는 경우"** 를 구분하지 못했다.
- "내 계좌 잔액 알려줘"는 애매한 요청이 아니라 PDF의 **"계좌 조회"** 기능 그 자체이므로 전체를 나열한다.
- 프로젝트 개요("사용자의 **자연어 요청**으로 처리하는 AI Agent")를 근거로, **자연어 해석은 딱딱한 Python 분기가 아니라 LLM이 맡는다**고 나도 판단했다.
- PDF의 "추가 질문"은 `interrupt`가 아니라 **평범한 대화 한 턴**으로 해결된다. `interrupt`는 되돌리기 어려운 작업의 **승인·거절**을 위한 도구다.

**그 결과**: 계좌 개수 분기와 `interrupt`를 제거하고, `account_call_model ↔ tools`를 오가는 **표준 ReAct 루프**로 단순화했다. tool 결과에 `nickname`을 포함시켰다.

---

## 3. 거래내역 조회 (대화 12)

### 3.1 나의 첫 생각 (원문)

> 거래 내역 조회도 잔액 조회처럼 supervisor agent가 사용자 요청 메시지를 account_call_model에게 전달하면 거래내역을 하는 도구를 이용해서 data.json의 transactions에서 필요한 데이터를 가져와서 보여주는 방식이 되야할 거 같아.

→ 구조는 잔액 조회와 같고 **tool 하나를 추가**하는 것으로 확인됨.

### 3.2 내 아이디어: CSV를 join한 종합 테이블 (원문)

> * 지금 있는 data.json을 각 key별로 .csv파일로 만든다. (accounts, bills, cards, transactions.csv...)
> * 이런 다음에 모든 csv파일들을 다  join해서 종합 table을 만들어서 그 table에는 모든 column name들을 다 포함시키면 사용자가 생활비로 어떤 기간동얀 거래내역을 물어보던 카드 사용 내역을 물어보던 다 해결될 거 같은데 안그런가?
>
> 이럴 때 오늘이 몇년 몇월 몇일인지 계산은 python이 하면 간단할 거 같은데 아닌가?

**AI 평가 → 내 결정**
- 날짜 계산을 Python이 한다는 생각은 **맞음**.
- 전부 join하면 안 됨: `bills`, `addresses`는 거래와 ID로 이어지지 않아 행이 복제됨. **`transactions` + `accounts`(별명) + `cards`(left join)만** join한다.
- CSV를 런타임 저장소로 쓰면 이체 등으로 `data.json`이 바뀔 때 낡은 데이터가 됨. **원본은 `data.json` 하나**로 두고 tool이 호출될 때 메모리에서 join한다. CSV는 데이터를 눈으로 보는 용도로만 남긴다.

### 3.3 내가 스스로 바로잡은 사례

Claude가 "카드와 계좌가 항상 같지는 않다"고 문제처럼 설명했을 때 나는 반문했다.

> 생활비 카드는 card-001인데 card-001로 생활비를 사용하면 user-001 account에서 돈이 빠져나가 … 너무 당연한거 같은데 뭐가 문제라는거지?

실제 데이터로 검증한 결과 카드와 계좌의 대응은 **완전히 일관**되어 있었고(어긋난 건 0건), 진짜 논점은 **"출금 전체(10건)"와 "카드 결제(9건)"의 범위가 다르다**는 것이었다. 그래서 `card_only` 옵션이 필요하다.

### 3.4 내가 정한 조회 조건 (원문)

> 기간, 계좌, 금액, 유형, 카드 결제 여부 이렇게 생각해봤어

### 3.5 "LLM이 view를 볼 수 있다"는 내 생각의 정정

> 별명과 owner_id가 주어지면 LLM이 account_id를 view를 보고 알아낼 수 있을거 같은데 아닌가?
> 내말은 view를 만들고 그 view에서 query를 할 수 있는 tool을 llm에게 binding하면 어떨까 하는거야

**정정**: LLM은 view를 볼 수 없고 **tool이 반환한 결과 문자열만** 본다. 그래서 (1) 잔액 조회 tool로 계좌 목록(`account_id`, `nickname`)을 먼저 받아 `account_id`를 알아내고 → (2) 거래내역 tool을 호출하는 **2단계 호출**을 채택했다. "view에 query하는 tool" 아이디어는 정해진 파라미터로 조건을 받는 방식 (가)와 LLM이 SQL을 직접 쓰는 방식 (나) 중 **(가)** 로 정했다.

### 3.6 내가 내린 결정 목록 (원문 인용)

| 결정 | 내 말 |
|---|---|
| 조건 전달 방식 | "(가)로 가자" |
| 기간 처리 | "기간은 B로 python이 계산하게 할게." |
| `period` 키워드 | "이 키워드 세트로 진행할게." (`this_month`, `last_month`, `this_week`, `last_week`, `today`) |
| 건수·합계 | "결과는 건수랑 합계도 파이썬이 계산해서 같이 돌려주자" |
| 0건일 때 | "0건이면 적용된 조건도 같이 돌려주자" |
| 잘못된 조건 | "잘못된 조건이면 오류 메시지를 반환하자" |
| tool 이름 | "get_account_transactions_by_owner로 하자" |
| 기준일 | "기준일은 2026-09-20으로 고정하고 파일에 넣어줘" |

### 3.7 확정된 tool 파라미터

`owner_id`, `account_id`, `period` **또는** `start_date`/`end_date`, `min_amount`, `max_amount`, `transaction_type`(`deposit`/`withdrawal`), `card_only`. 전부 선택이며 `None`이면 그 조건은 걸지 않는다. 처리 순서는 입력 검증 → `period`를 날짜로 변환 → view 생성 → **`owner_id` 필터를 가장 먼저** → 나머지 필터 → 최근순 정렬 → 건수·유형별 합계 계산이다.

---

## 4. 내 계좌 간 이체 (대화 15, 16)

### 4.1 범위 결정

> 내 계좌 간 이체만 1단계로 하자

스펙의 이체 예시가 전부 내 계좌 간이었고, 스펙에 명시된 규칙은 "출금 계좌는 본인 것이어야 한다"(선생님 말씀) 하나뿐이었다. 타인 계좌 송금은 2단계로 미룬다. 1단계에서는 **출금·입금 계좌 모두 본인 소유**여야 한다.

### 4.2 1차 초안 (내가 직접 작성, 원문)

> 이체 설계 초안을 제시한다.
>
> * 사용자(user-001)로부터 "내 저축계좌에서 여행계좌로 500,000원 지금 이체해줘."라는 요청이 들어온다.
> * supervisor가 이걸 account_agent에게 전달한다.
> * 기존의 account_tools에 get_account_transfer_by_owner라는 새로운 tool을 포함시킨다.
> * get_account_transfer_by_owner에서는 data.json[''accounts"]에서 acc-002의 dictionary에서 balance를 500,000원 감소시키고, acc-003의 dictionary에서 balance를 500,000원 증가시킨다. 또한 data.json['transactions']에서 이에 mapping하는 withdrawal과 deposit를 한건씩 발생시킨다.
> * get_account_transfer_by_owner는 from, to, amount, date를 dictionary로 반환한다.
> * 이 과정에서 decision=interrupt({"question":..., "tool":"get_account_transfer_by_owner", "args": ...})를 집어넣어 사용자의 이체 승인/거절 의사를 확인하는 과정을 거친다.

**AI 피드백 (요약)**: 승인이 변경보다 **먼저**여야 하고(`interrupt`는 재개 시 그 노드를 처음부터 다시 실행하므로 앞에는 읽기 전용 검증만 둔다), 검증·거절·실패 분기, 원자적 저장, State 필드와 변경 시점, 승인 화면 내용, 거래 시각이 빠져 있다.

### 4.3 2차 초안 (내가 직접 작성, 원문)

> * account_call_model이 이 요청은 자금이체에 관한 거니까 llm_with_tools가 binding된 tools 중 에서 get_account_transfer_by_owner(가칭)에 대한 tool_calls를 한다.
> * get_account_transfer_by_owner는 이체에 대해 먼저 이런 이체요청에 잘못된 것이 있는지 다음 상항들에 대해서 검증한다. 출금,입금계좌가 모두 로그인한 사용자의 것인지, 두 계좌가 다른 계좌인지, 잔액은 충분한지, 계좌가 존재하는지)
> * 만약 이런 검증 과정에서 이상이 발견된다면  이체가 불가능한 이유를 account_call_model에게 반환한다.
> * 만약 검증 과정이 통과된다면 그 다음 과정인 이체에 관한 사용자 승인/거절에 대한 응답을 interrupt 을 이용해서 진행한다.
> * 사용자가 이체를 거절한다면 그 결과를 interrupt에 보낸다
> * 사용자가 이체를 승인한다면 그 결과를 interrupt에 보낸다.
> * get_account_transfer_by_owner의 반환 값인 이체 결과를 보여주는 dictionary를 account_call_model은 받아서 사용자에게 전달한다.

**AI 피드백 (요약)**: 순서와 검증은 개선됨. 5·6번은 방향이 반대다(`interrupt`가 답을 "받고", 그 답을 넣어주는 주체는 `main.py`의 `Command(resume=…)`). 승인 뒤의 변경 단계, 금액 검증, State, 승인 화면이 아직 비어 있다. (내가 "2번(변경 단계)은 이미 초안에서 말해서 생략했고, 3번(금액 검증)은 실수로 빠졌다"고 설명함.)

### 4.4 State 스케치 (내가 직접 작성, 원문)

```python
class TransferAccounts(BaseModel):
    owner_id: str = Field(description="이체 계좌들의 소유자 id")
    from_account = str: Field(description="이체로 자금이 빠져나가는 account_id")
    to_account: str = Field(description="이체로 자금이 들어가는 account_id")
    from_nickname: str = Field(description="이체로 자금이 빠져나가는 account의 nickname")
    to_nickname: str = Field(description="이체로 자금이 들어가는 account의 nickname")
    amount: int = Field(description="이체 금액")
    date: datetime = Field(description="이체 시각")


class BankState(TypedDict):
    ...
    transfer: TransferAccounts

@tool(parse_docstring=True)
def get_account_transfer_by_owner(owner_id: str, ....) -> TransferAccount:
    ....    
```

**AI 피드백 (요약)**: 구조화된 객체로 이체 내용을 다루는 발상은 좋다. 그러나 **tool의 반환값은 State에 들어가지 않으므로** `State.transfer`를 누가 채우는지가 비어 있다 → (A) tool 안에서 전부 처리 vs (B) 노드로 쪼개 State로 연결 중 선택해야 한다. 필드는 `owner_id`(LLM이 채우면 신뢰 불가), 별명(코드가 조회 가능한 파생 값), `date`(LLM은 지금을 모름)를 정리해야 한다.

### 4.5 흐름도 초안 (내가 직접 작성, 원문)

> 사용자 요청 -> START -> supervisor -> transfer_agent -> get_transfer_account_by_owner 도구 실행 -> 검증 절차 진행 -> if fail: return fail with reason elif pass: return 메시지와 함께 TransferAccounts 객체 return -> 사용자에게 승인/거절 요청: if reject: return fail with reason elif approve: return pass message with TransferAccounts 객체.

**AI 피드백 (요약)**: "도구 실행 → 검증"은 (A) 방식의 그림이라 (B)와 어긋난다. **흐름이 승인 직후에 끝나서 돈이 실제로 움직이는 단계가 없다**(1·2차 초안에서도 반복). 종료 경로가 실패·거절 두 개뿐이다. 노드별 "읽는 것 / 쓰는 것 / 다음(조건별)" 표를 채우도록 제안받았다.

### 4.6 내가 내린 결정 (원문 인용)

> (B)로 갈게. 노드로 쪼개서 State로 연결하자
> TransferAccounts에서 owner_id는 빼는게 맞아. 그렇게 할게.
> from_nickname, to_nickname도 account_id로 조회해서 가져오면 되니까 뺴기로 하자.
> date는 실행 시점에 python code가 채우도록 하자
> amount도 Field(gt=0)로 schema에 넣고 원 단위 정수 여부와 함께 검증 노드에서 다루도록 하자.
>
> (가)로 갈게 *(이체 전용 서브그래프 `transfer_agent`를 만들고 supervisor가 새 목적지로 라우팅)*

`State.transfer`를 **언제 채우고 언제 비우는지**를 내가 물었다.

> transfer가 언제 채워지는가, 언제 비워지는가는 get_account_transfer_by_owner 도구가 사용자 승인/거절도 다 끝난 다음 완전히 실행된 시점이어야 하는거 아니니? 잘 모르겠으니 네 생각을 말해줘

**정리된 답**: **채우는 건 흐름의 맨 처음**(승인 노드가 보여줄 내용을 알아야 하고, `interrupt`로 멈춘 동안 이 값을 기억하는 곳이 State), **비우는 건 이 흐름이 어떤 경로로든 끝날 때**(성공뿐 아니라 검증 실패, 거절, 저장 실패도 종료)이다.

### 4.7 승인 흐름: 승인 전 수정 + 자연어 승인·취소 (2026-09-25)

남은 과제 중 "승인 전 수정, 자연어 승인·취소"를 (가) 승인 흐름 개선으로 묶어 진행하기로 했다(스펙: "처리안을 수정하고 전체 내용을 다시 보여준 뒤 승인받는다", 예 "아니, 5만 원만" / 자연어 응답을 승인 또는 취소로 연결, 예 "응, 진행해" / "취소할게").

Claude가 결정 지점 6가지(수정 대상, 응답 해석 위치, 되돌아가는 방식, 수정 반영 방법, 수정 후 검증 실패, 그 밖의 경우)를 먼저 제시했고, 나는 다음과 같이 답했다.

**내 답변 (원문)**

> (가)로 하자. 승인 전 수정부터 설계해볼게
>
> 1번: 금액뿐만 아니라 출금, 입금 계좌까지 수정대상으로 하는게 현실감이 더 있겠어.
> 2번: 응답 해석이라는 하나의 단계로 하자. 내 생각엔 자연어 해석을 llm이 판단한다는게 지금까지의 우리가 이 프로젝트를 설계 구현하면서 지켜왔던 방침으로 알고 있어. 앞으로도 계속 이 방침을 이어가면 해.
> 3번: 이건 내가 잘 모르겠어. 너의 도움이 필요해
> 4번은 수정하는 tool의 return값을 amount에 대한 dictionary로 만들면 될거 같은데...
> 5번: 네 생각을 말해줘.

**AI 피드백 (요약)**
- 1번·2번에 동의. 다만 승인은 돈이 움직이기 직전의 마지막 관문이므로 LLM이 해석하더라도 **코드가 지킬 규칙 두 개**를 넣자고 제안했다: (1) 애매하면 승인하지 않는다, (2) 수정은 절대 승인을 겸하지 않는다("5만 원으로 하고 진행해"도 수정만 반영하고 새로 승인받는다).
- **4번은 정정**: 이체 흐름은 (B)로 tool 없이 노드로 만들기로 했고 tool의 반환값은 State에 들어가지 않으므로, 수정도 `transfer_extract`처럼 `with_structured_output`을 쓰는 노드여야 한다. "바뀐 것만 담는 dictionary"라는 발상은 그대로 살려 선택 필드로 병합한다.
- **3번 제안**: `transfer_approve`(화면 표시, 응답 원문 저장) → `transfer_interpret`(LLM 분류) → approve/reject/edit/unclear로 분기하고 edit·unclear는 `transfer_approve`로 **엣지를 따라 되돌아가는 순환**으로 만든다. 순환의 모든 바퀴가 `interrupt`(사람의 입력)를 거치므로 **무한 수정은 안전 문제가 아니다**(자동으로 폭주할 수 없음).
- **5번 제안**: 수정 후 검증이 실패하면 흐름을 종료하지 않고 **이전 안을 유지한 채 이유를 알려 주고 승인 화면으로 돌아간다.** 승인 화면에 오는 이체안은 항상 검증을 통과한 안이라는 불변 조건이 생긴다.

### 4.8 승인 흐름 초안 (내가 직접 작성 — AI 제안을 상당 부분 반영한 버전, 원문)

> 승인 흐름에 관한 설계안에 대해 초안을 작성한다.
>
> * 금액뿐만 아니라 계좌도 수정 대상으로 한다. 이에 따라 수정된 이체안은 소유권, 같은 계좌,  잔액 검증을 다시 거쳐야 한다. nickname을 account_id로 변경하는 방식도 transfer_extract와 같은 방식을 재사용한다.
> * LLM이 응답을 해석하고 코드는 계산, 검증, 변경한다는 우리의 방침에 따라 사용자가 응답한 내용이 승인인지, 거절인지, 수정인지는 LLM이 하되 이체의 승인은 돈이 움직이는 마지막 관문이라서 LLM이 해석을 하더라도 코드가 지킬 두가지 규칙을 다음과 같이 넣는다. (애매하면 승인하지 않는다. 수정은 절대로 승인을 겸하지 않는다. "5만원으로 하고 진행해" 처럼 수정과 승인이 섞인 말도 수정만 반영하고 바뀐 전체 화면을 보여준 뒤 새로 승인을 받는다.
> * 수정한 다음에는 transfer_extract처럼 with_structured_output을 사용해서 return값을 만든다.
> * 수정안을 어떻게 되돌릴까에 대한 설계는 claude의 의견을 다음과 같이 반영했다.
> *
>
> ```
> transfer_approve    전체 화면을 interrupt로 보여줌 → 사용자의 응답 원문을 State에 저장
>       ↓
> transfer_interpret  LLM이 응답을 분류 (approve / reject / edit / unclear)
>       ├ approve → transfer_execute
>       ├ reject  → 종료 (취소)
>       ├ edit    → 수정안을 병합하고 코드로 검증
>       │            ├ 유효 → transfer_approve (바뀐 전체 화면)
>       │            └ 무효 → 이전 안 유지 + 이유 → transfer_approve
>       └ unclear → transfer_approve (다시 묻기)
> ```
>
> * 수정 후 검증이 실패하는 경우에 대한 설계도 claude의 의견을 다음과 같이 반영했다. ""이전 안을 유지하고, 이유를 알려준 뒤 승인 화면으로 돌아간다"를 추천합니다. 이체 흐름을 통째로 종료하는 방식은 승인 전 검증 실패와 일관되지만, 사용자가 "500만 원으로"라고 한 번 잘못 말했다고 전체를 다시 시작해야 하니 불편합니다. 안전성은 똑같아요. 데이터 변경은 어차피 transfer_execute에서만 일어나고, 거기서 재검증까지 하니까요.
>
> 이 방식의 장점은 "승인 화면에 오는 이체안은 항상 검증을 통과한 안"이라는 불변 조건이 생긴다는 점입니다. 수정 검증을 해석 노드 안에서 하면 transfer_validate로 되돌아갈 필요도 없어져요."
>
> 여기까지 네가 짜준 설계안을 많이 반영해서 만들어 봤어.
> 네가 추가로 초안에 담아야 할 것이라고 한 내용들인
>
> State: 응답 원문과 분류 결과, 수정 실패 이유를 다음 노드에 전달하려면 통로가 필요합니다. 어떤 필드가 새로 필요한지 직접 찾아보세요. tool 반환값이 State에 못 들어간다는 것과 같은 종류의 문제예요.
> * `main.py`와 `interrupt` 사이에 오가는 값: 지금은 `'approve'`/`'reject'`인데, 이제 사용자가 친 원문 그대로가 넘어가게 되는지.
> * 노드별 "읽는 것 / 쓰는 것 / 다음" 표와 종료 경로

**이 초안의 상태(솔직하게)**: 순환 구조와 두 가지 코드 규칙, 수정 후 검증 실패 시 이전 안 유지는 **Claude의 제안을 받아들여 내가 다시 쓴 것**이다. 그리고 초안은 마지막 항목이 빈 줄로 끝났고, Claude가 담으라고 한 **세 가지(State 필드, `main.py`↔`interrupt` 값, 노드 표)는 채우지 못했다.**

### 4.9 내가 못한 부분과 위임 (솔직한 대화, 원문)

Claude는 초안에 남은 빈칸에 더해, 초안이 답하지 않는 경우 네 가지를 질문으로 짚었다: 분류가 실패하면 어떻게 하는지 / `edit`인데 바뀐 값이 없거나 현재와 같으면 어떻게 하는지 / `unclear`일 때 사용자에게 무슨 말을 하는지 / 정확히 "승인"이면 LLM 없이 바로 승인하는 빠른 경로를 둘지. 이에 대한 나의 답:

> 초안에 더 다뤄야 할 경우들을 어떻게 처리해야 하는지에 관한 설계는 아직 내가 능력이 부족해서 못할거 같아.
> 이제는 이런 너와 나의 솔직한 대화를 이미 작성한 "설계초안.md"에 추가로 저장해주고 이 작업에 대한 너의 설계안을 제시해주고 직접 파일에 구현해줘.

**정리**: 이 부분은 내가 초안을 직접 쓰지 못했고, **Claude에게 설계와 구현을 위임**했다. 4.10의 설계안은 Claude가 제시한 것이며 나는 읽고 이해한 뒤 질문하기로 했다. 최종 설계 자료와 회고에서도 이 점을 그대로 적는다.

### 4.10 Claude가 제시하고 구현한 설계안 (내가 위임한 부분)

**흐름**

```
transfer_approve   화면을 interrupt로 보여줌 → 사용자의 응답 "원문"을 State에 저장
      ↓
transfer_interpret ① 정확히 "승인"/"거절"이면 LLM 없이 즉시 처리 (결정론적 빠른 경로)
                   ② 그 밖에는 LLM이 분류(approve / reject / edit / unclear) 후, 코드가 규칙 적용
      ├ approve → transfer_execute (재검증 후 변경·저장)
      ├ reject  → 종료(취소)
      ├ edit    → 수정안 병합 + 검증
      │            ├ 유효 → transfer_approve (바뀐 전체 화면, "수정 반영" 안내)
      │            └ 무효 → 이전 안 유지 + 이유 안내 → transfer_approve
      └ unclear → transfer_approve (이유 안내 후 다시 묻기)
```

**빈칸에 대한 결정 (Claude)**

| 질문 | 결정 |
|---|---|
| State 새 필드 | `transfer_response`(응답 원문), `transfer_decision`(분류 결과), `transfer_notice`(다음 화면 맨 위에 보일 안내). tool이 아니라 State 필드로 노드 사이를 전달 |
| `main.py` ↔ `interrupt` | 사용자가 친 **원문 그대로** 전달. 엄격한 "승인/거절" 검사는 제거하고 해석은 그래프가 한다 |
| 분류 실패(LLM 오류, None) | `unclear`로 취급 → **절대 승인하지 않음** |
| 승인인데 수정 값도 있음 | LLM이 approve라고 해도 **코드가 수정으로 강제 변경**(규칙 2를 LLM이 아닌 코드가 지킴) |
| `edit`인데 값이 없음 / 현재와 동일 | `unclear` + 안내 문구 |
| 빠른 경로 | 정확히 "승인"/"거절"만 LLM 없이 처리(앞뒤 공백 허용, "승인합니다"는 LLM이 해석) |
| `unclear`일 때 | 왜 다시 나왔는지 화면 맨 위에 안내 |
| 실행 방어 | `transfer_execute`는 `transfer_decision == 'approve'`가 아니면 실행 거부 |
| 종료 시 정리 | 모든 종료 경로에서 `transfer`와 새 필드 3개를 함께 비움(`transfer_end` 공통 함수) |
| 무한 수정 | 제한을 두지 않음: 순환의 모든 바퀴가 `interrupt`(사람의 입력)를 거치므로 폭주 위험이 없다 |

**노드별 표**

| 노드 | 읽는 것 | 쓰는 것 | 다음 |
|---|---|---|---|
| `transfer_approve` | `transfer`, `owner_id`, `transfer_notice` | `transfer_response`(원문), `transfer_notice=None`, `transfer_decision=None` | 항상 `transfer_interpret` |
| `transfer_interpret` | `transfer_response`, `transfer`, `owner_id` | `transfer_decision`, (수정 성공 시) `transfer`, `transfer_notice`, `transfer_response=None` | approve → execute / edit·unclear → approve / reject → END |
| `transfer_execute` | `transfer_decision`, `transfer`, `owner_id` | 결과 메시지, 이체 State 전부 비움 | 항상 END |

**구현 (파일)**: `src/graph.py`(State 3필드, `ResponseInterpretation`, `classify_response`, `make_transfer_interpret`, 바뀐 `transfer_approve`, `transfer_execute` 방어, `transfer_end`, 순환이 들어간 `build_transfer_graph`) / `src/main.py`(응답 원문 전달) / `src/test_transfer.py`(테스트 추가).

**설계 포인트**: LLM 분류기를 함수로 주입할 수 있게 만들어(`make_transfer_interpret(classifier)`) **분류 뒤의 규칙(병합·검증·이전 안 유지·승인+수정 강제 변경·분류 실패 처리)을 LLM 없이 결정론적으로 테스트**할 수 있다.

**검증 1 — LLM 없이 (`src/test_transfer.py`, 107개 통과, 실제 `data.json` 불변)**: 빠른 경로가 LLM을 호출하지 않음 / 자연어 승인·취소 / 금액·계좌 수정이 전체 화면에 반영되고 수정만으로는 데이터가 바뀌지 않음 / 수정 실패 4종(잔액 초과, 남의 계좌, 같은 계좌, 0원 이하)은 이전 안을 유지하고 이유를 안내하며 이후 승인하면 **원래 안**으로 실행 / **승인+수정 혼합은 실행되지 않음** / unclear·LLM 오류·None은 승인하지 않음 / 값 없는 수정·동일 수정은 다시 묻기 / 수정을 여러 번 한 뒤 승인하면 마지막 안으로 한 번만 실행 / 수정 도중 거절 / 승인 확인 없이 실행 노드를 직접 부르면 거부 / 모든 종료 경로에서 이체 State 비워짐.

**검증 2 — 실제 LLM 전체 흐름 (임시 데이터, 10개 시나리오)**

| 시나리오 | 결과 |
|---|---|
| A. "50만 원" 요청 → "아니, 5만 원만" → "응, 진행해" | 화면이 5만 원으로 바뀌고, 자연어 승인으로 **50,000원** 이체 |
| B. 입금 계좌를 "저축 말고 여행 자금으로" | 입금 계좌 수정이 반영되어 생활비 → 여행 자금 |
| **C. "3만 원으로 하고 진행해"(승인+수정)** | **수정만 반영하고 실행하지 않음**(거래 20건 유지, 승인 대기). 새로 "승인"하면(C2) 30,000원 실행 |
| D. "금액을 1000만 원으로 바꿔줘" | "반영할 수 없습니다: 잔액 부족"과 함께 **이전 안(100,000원) 유지** |
| E. "음... 잘 모르겠어" → "취소할게" | 이해하지 못했다고 안내 후 다시 묻고, 자연어 취소로 종료 |
| F. **"승인하지 마"** | 승인으로 착각하지 않고 **취소** |
| G. "그래 좋아, 해줘" | 자연어 승인으로 실행 |
| H. "15만 원으로" → "입금은 여행 자금으로" → "네 진행해주세요" | 두 번 수정이 누적되어 마지막 안으로 한 번만 실행 |
| I. "입금 계좌를 acc-004로 바꿔줘" | 남의 계좌라 이전 안 유지 + 안내 |

**검증 3 — `main.py` 입력 루프**: 빈 입력은 다시 묻고, "아니, 3만 원으로 해줘"가 수정으로 처리되어 바뀐 화면이 나오며, "승인"으로 30,000원이 실행되고, 이어서 승인 대기 중 `quit`하면 실행 없이 종료된다.

**이 설계에서 배운 점**
1. **LLM이 해석하는 곳일수록 코드가 마지막 문을 지켜야 한다.** 분류가 틀려도(승인이라고 했는데 수정 값이 있음, 분류 실패) 결과가 안전하도록 규칙을 LLM 바깥에 두었다.
2. **기본값은 "실행하지 않음"이다.** 애매함, 실패, 값 없음, 알 수 없는 분류는 모두 `unclear`로 모여 다시 묻는다.
3. **순환이 위험하지 않은 이유를 따져 본다.** 자동 반복이 아니라 매 바퀴가 사람의 입력을 기다리는 구조라 횟수 제한이 필요 없다.
4. **서브그래프가 멈춰 있는 동안 서브그래프 안의 State는 부모의 `values`에 아직 없다.** 테스트에서 멈춘 상태의 State를 보려면 `get_state(..., subgraphs=True)`로 서브그래프 스냅샷을 읽어야 했다.

---

## 5. 초안 기준 요약: 흐름·State·분기 조건

> PDF가 설계 초안에 요구한 세 가지(요청부터 종료까지의 흐름, 주요 State 필드와 변경 시점, 조회·변경·승인·거절·실패의 분기 조건)를 위 초안과 결정을 모아 정리한 것이다. 구현 과정의 변경은 "최종 설계 자료"에서 다룬다.

### 5.1 전체 흐름

```
사용자 요청 → START → supervisor ─┬→ account_agent → END      (잔액 조회, 거래내역 조회)
                                  └→ transfer_agent → END     (내 계좌 간 이체)
```

**조회(account_agent)**: `account_call_model` ↔ `tools`(ReAct 루프). LLM이 tool을 고르고 결과를 읽어 답한다. tool 호출 직전 `account_enforce_owner`가 `owner_id`를 State의 값으로 강제로 교체한다.

**이체(transfer_agent)**:

```
transfer_extract ─(정보 부족/금액 오류)→ [종료: 되묻기]
  └→ transfer_validate ─(실패)→ [종료: 이유 안내]
       └→ transfer_approve (interrupt로 화면 표시, 응답 원문을 State에 저장)
            └→ transfer_interpret (LLM이 승인/거절/수정/불명확으로 분류, 규칙은 코드가 적용)
                 ├ reject → [종료: 취소, 변경 없음]
                 ├ edit → 수정안 병합·검증 ─ 유효/무효 모두 → transfer_approve (전체 화면 다시)
                 ├ unclear → transfer_approve (이유 안내 후 다시 묻기)
                 └ approve → transfer_execute ─(승인 확인 없음/재검증 실패)→ [종료]
                                              ├(저장 실패)→ [종료: 변경 없음]
                                              └(성공)→ [종료: 결과 안내]
모든 종료에서 이체 관련 State(transfer 등 4개)를 비운다
```

### 5.2 State 필드와 변경 시점

| 필드 | 값 | 채워지는 시점 | 비워지는 시점 |
|---|---|---|---|
| `messages` | 대화 이력(`add_messages`로 누적) | 매 턴 | 없음(세션 내내 유지) |
| `owner_id` | 로그인한 사용자 | 첫 `invoke` 때 `main.py`가 한 번 넘김 | 바뀌지 않음 |
| `next` | supervisor의 라우팅 결과 | `supervisor` 노드 | 매 요청마다 덮어씀 |
| `transfer` | 이체 내용(`from_account`, `to_account`, `amount`, `date`) | `transfer_extract` (수정이 유효하면 `transfer_interpret`가 갱신, `date`는 `transfer_execute`가 채움) | 이체 흐름이 **어떤 경로로든 끝날 때** |
| `transfer_response` | 승인 화면에 대한 사용자의 응답 원문 | `transfer_approve` | `transfer_interpret`가 읽은 직후, 종료 시 |
| `transfer_decision` | 분류 결과(`approve` / `edit` / `unclear`) | `transfer_interpret` | 다음 `transfer_approve` 진입 시, 종료 시 |
| `transfer_notice` | 다음 승인 화면 맨 위에 보일 안내(수정 반영, 수정 불가 이유, 이해하지 못함) | `transfer_interpret` | `transfer_approve`가 화면에 보여준 뒤, 종료 시 |

### 5.3 분기 조건

| 상황 | 조건 | 결과 |
|---|---|---|
| **조회** | 항상 실행 가능(본인 데이터만) | 결과 나열. 없는 계좌·남의 계좌는 "조회할 수 없는 계좌"로 동일하게 응답 |
| **조회 실패** | 기간·금액 조건이 잘못됨(시작>종료, 음수, `period`와 날짜 동시 지정 등) | 오류 메시지 반환 |
| **0건** | 조건은 정상인데 해당 거래 없음 | 적용된 조건과 함께 "내역 없음" 안내 |
| **변경(이체) 진입** | 이체 정보(출금·입금 계좌, 금액)가 다 있음 | 검증으로 진행 |
| **정보 부족** | 세 항목 중 하나라도 없음 | 부족한 항목을 되묻고 종료 |
| **검증 실패** | 금액이 0 이하이거나 정수가 아님 / 남의 계좌 또는 없는 계좌 / 두 계좌가 같음 / 출금 계좌 잔액 부족 | 이유 안내, **승인 요청 없이** 종료 |
| **승인** | 사용자가 "승인" | 실행 노드가 재검증 후 변경·저장 |
| **거절** | 사용자가 "거절" | 변경 없이 종료 |
| **재검증 실패** | 승인 대기 중 잔액이 바뀜 | 이유 안내, 변경 없이 종료 |
| **저장 실패** | 파일 저장 중 오류 | 이체가 반영되지 않았음을 안내, 데이터 불변 |
| **승인 전 수정** | 응답이 계좌·금액을 바꾸겠다는 뜻(`edit`) | 수정안을 병합·검증해 유효하면 **바뀐 전체 화면**을 다시 보이고 새로 승인받음(데이터는 아직 불변) |
| **수정 후 검증 실패** | 병합한 수정안이 검증에 걸림(잔액 초과, 남의 계좌, 같은 계좌, 0원 이하) | **이전 안을 유지**하고 이유를 안내한 뒤 승인 화면으로 복귀 |
| **승인과 수정이 섞인 응답** | "3만 원으로 하고 진행해"처럼 분류가 approve여도 수정 값이 있음 | 코드가 **수정으로 강제 변경**, 수정만 반영하고 새로 승인받음 |
| **애매한 응답 / 분류 실패** | `unclear`, LLM 오류, 값 없는 `edit`, 현재와 같은 값 | **승인하지 않고** 이유를 안내한 뒤 다시 묻기 |
| **빠른 경로** | 응답이 정확히 "승인" 또는 "거절" | LLM 없이 즉시 승인/취소 |
| **승인 확인 없는 실행 시도** | `transfer_decision`이 `approve`가 아님 | `transfer_execute`가 실행을 거부 |

---

## 6. AI 활용 내역 (정직한 기록)

과제는 "설계 초안은 AI 없이 직접 작성하고, 이후 AI로 검토·보완할 수 있다"고 했다. 실제로 있었던 일은 다음과 같다.

| 항목 | 실제 진행 |
|---|---|
| **잔액 조회·거래내역 조회 설계** | 처음부터 Claude와 **대화하며** 설계했다. 위 2·3장의 원문은 그 대화 중 내가 쓴 것이지 "AI 없이 미리 쓴 초안"은 아니다. Claude가 결정 지점과 선택지를 제시하고 내가 선택·반문했다. |
| **이체 설계** | Claude가 **고려할 결정 지점 체크리스트를 먼저** 제시했고, 나는 그것을 보고 1차 초안을 **직접** 썼다. 그 뒤 피드백을 받아 2차 초안, State 스케치, 흐름도 초안을 직접 다시 썼다. 원문은 4장에 보존했다. |
| **승인 흐름 설계** (승인 전 수정, 자연어 승인·취소) | Claude가 결정 지점 6가지를 제시했고, 나는 1번(계좌도 수정)과 2번(LLM이 응답 해석)에 답하고 3번(되돌아가는 방식)과 5번(수정 후 검증 실패)은 도움을 요청해 **Claude의 제안을 받아들여** 초안을 다시 썼다(4.8). 초안이 다루지 못한 State 필드, `main.py`↔`interrupt` 값, 노드 표, 예외 상황은 **내가 직접 설계하지 못하고 Claude에게 설계와 구현을 위임**했다(4.9, 4.10). |
| **피드백** | Claude가 옳고 그름을 지적했다(예: 승인 전 변경 금지, `interrupt` 방향, tool 반환값은 State에 안 들어감). 나는 이를 검토해 채택하거나 반문했다. 반문으로 Claude의 설명이 정정된 적도 있다(3.3). |
| **AI가 먼저 낸 아이디어를 내가 채택한 것** | 결정론적 방어(`account_enforce_owner`), 원자적 저장, 승인 후 재검증, 종료 경로마다 State 정리 |
| **구현** | **코드는 Claude가 작성**했다. 나는 코드를 읽고 이해한 뒤 질문하기로 했고, 이체 구현은 흐름도를 직접 완성하는 대신 "이런 내용을 다 반영해서 작업을 완성해줘"라고 **위임**했다. |
| **검증** | 테스트는 Claude가 작성·실행했고, 나는 `main.py`로 직접 여러 요청을 실행해 결과를 데이터와 대조했다. |

> 이 표는 README와 회고에도 같은 내용으로 정직하게 옮겨 적는다.

---

## 7. 남은 설계 과제 (미결정)

- 조건부 이체("생활비에 40만 원 남기고 나머지를 저축"), **분할 이체**(여러 계좌로, 전체 원자적 처리), 계좌 별명 변경 (승인 전 수정, 자연어 승인·취소는 4.7~4.10에서 완료)
- 후속 결과 조회("아까 이체가 됐어?"): 거절·실패 기록도 `requests`에 남길지
- 카드(조회·잠금·분실 정지·재발급 등), 청구서(조회·납부·일괄 납부)
- 재시작·장애 복구: `InMemorySaver`는 프로그램을 끄면 사라지므로 영속 저장소 필요
- 잔액 합산을 Python tool로 만들지(현재는 LLM이 암산)
- 이체 세부 사항 8개(거래 시각, 거절·실패 기록 등)의 검토 → `mini_project_issues_talking.md` 대화 16 참고

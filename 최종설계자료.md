# 최종 설계 자료 — 가상 금융 업무 AI Agent

> 실제 구현에 맞게 갱신한 설계와, **설계 초안에서 바뀐 내용과 그 이유**를 정리한 문서다.
> 초안 원문과 AI 피드백은 [`설계초안.md`](설계초안.md), 과정 전체 기록은 [`mini_project_issues_talking.md`](mini_project_issues_talking.md)에 있다.
> 실행 방법과 동작 확인 결과는 [`README.md`](README.md)에 있다.

---

## 1. 범위

| 구분 | 내용 |
|---|---|
| 구현 | 계좌 목록·잔액 조회, 거래내역 조회(기간·금액·유형·카드 결제), **내 계좌 간 즉시 이체**, 승인·거절, 승인 전 수정, 자연어 승인·취소, 추가 질문(되묻기), 지원하지 않는 요청 안내 |
| 미구현 | 카드 기능 전체, 청구서 기능 전체, 조건부 이체, 분할 이체, 계좌 별명 변경, 후속 결과 조회, 대화 대상 확인, 재시작·장애 복구, 타인 계좌 송금 |

이체는 **출금·입금 계좌가 모두 본인 소유**인 경우만 다룬다(스펙의 예시가 전부 내 계좌 간이었고, 스펙에 명시된 규칙은 "출금 계좌는 본인 것이어야 한다" 하나뿐이었다).

## 2. 최종 구조

```
사용자 요청 → START → supervisor ─┬→ account_agent  (서브그래프) → END
                                  ├→ transfer_agent (서브그래프) → END
                                  └→ unsupported    (고정 안내문)  → END
```

- 부모 그래프와 두 서브그래프는 **같은 State(`BankState`)를 공유**하고, checkpointer(`InMemorySaver`)는 **부모를 compile할 때만** 설정한다. 서브그래프 안의 `interrupt`도 부모 결과의 `__interrupt__`로 나타난다.
- **역할 분담 원칙**: 자연어 해석은 LLM, 계산·필터·정렬·검증·데이터 변경은 코드가 한다. LLM이 해석하는 곳(승인 응답, 라우팅)에서도 **마지막 규칙은 코드**가 지킨다.

### 2.1 supervisor

`with_structured_output(RouterDecision)`으로 `account_agent` / `transfer_agent` / `unsupported` 중 하나를 고른다. 이체 정보를 되물은 직후 사용자의 답도 `transfer_agent`로 보내도록 프롬프트에 명시했고, 카드로 결제한 "거래내역" 조회는 `account_agent`, 카드 자체의 정지·재발급·목록은 `unsupported`로 구분해 명시했다.

### 2.2 account_agent (조회)

```
account_call_model ─(tool 호출 있음)→ account_enforce_owner → tools → account_call_model
        └─(tool 호출 없음)→ END
```

- `account_call_model`: system 프롬프트에 `owner_id`, 기준일, 처리 지침을 넣고 tool을 고르거나 최종 답을 쓴다.
- `account_enforce_owner`: LLM이 만든 `tool_calls`의 `owner_id`를 **State의 값으로 강제 교체**한다(`add_messages`가 같은 `id`의 메시지를 교체하는 동작을 이용). LLM이 무엇을 넣어도 결과가 같은 결정론적 방어다.
- tool 2개: `get_account_balance_by_owner`, `get_account_transactions_by_owner`.

**거래내역 tool** (`get_account_transactions_by_owner`)
- 파라미터(전부 선택): `account_id`, `period`(`this_month`/`last_month`/`this_week`/`last_week`/`today`) 또는 `start_date`·`end_date`, `min_amount`, `max_amount`, `transaction_type`(`deposit`/`withdrawal`), `card_only`.
- 처리 순서: 입력 검증 → `period`를 실제 날짜로 변환(코드) → `data.json`에서 `transactions`+`accounts`(별명)+`cards`(left join)를 메모리에서 join → **`owner_id` 필터를 가장 먼저** → 나머지 필터 → 최근순 정렬 → 건수·유형별 합계 계산.
- 반환: 성공은 `count`, `total_deposit`, `total_withdrawal`, `applied_filters`(적용된 조건, `period`는 실제 날짜로 풀린 값), `transactions`. 조건이 잘못되면 `{"error": "..."}`만 반환한다. 남의 계좌와 없는 계좌는 같은 메시지("조회할 수 없는 계좌")다.
- **기준일은 2026-09-20으로 고정**(`BASE_DATE`, 초기 데이터의 마지막 거래일). system 프롬프트에도 같은 값을 넣어 연도 없는 날짜의 연도를 기준일 연도로 해석하게 한다.

### 2.3 transfer_agent (이체)

```
transfer_extract ─(정보 부족·금액 오류)→ [종료]
   └→ transfer_validate ─(실패)→ [종료]
        └→ transfer_approve  (interrupt로 전체 화면 표시, 응답 원문을 State에 저장)
             └→ transfer_interpret
                  ├ reject  → [종료]
                  ├ edit    → 병합·검증 → transfer_approve (유효/무효 모두 전체 화면 다시)
                  ├ unclear → transfer_approve (이유 안내 후 다시 묻기)
                  └ approve → transfer_execute ─(실패)→ [종료]
                                              └(성공)→ [종료]
모든 종료에서 이체 관련 State 4개를 함께 비운다
```

| 노드 | 읽는 것 | 쓰는 것 | 다음 |
|---|---|---|---|
| `transfer_extract` | `messages`, `owner_id` (+ 코드가 조회한 본인 계좌 목록을 프롬프트에 주입) | `transfer`를 채움(다른 3개는 비움). 부족·오류면 안내 메시지와 함께 전부 비움 | 채워짐 → validate / 아니면 END |
| `transfer_validate` | `transfer`, `owner_id` | 통과 시 없음. 실패 시 이유 메시지 + 전부 비움 | 통과 → approve / 실패 → END |
| `transfer_approve` | `transfer`, `owner_id`, `transfer_notice` | `transfer_response`(응답 원문), `transfer_notice=None`, `transfer_decision=None` | 항상 interpret |
| `transfer_interpret` | `transfer_response`, `transfer`, `owner_id` | `transfer_decision`, (수정이 유효하면) `transfer`, `transfer_notice`, `transfer_response=None` | approve → execute / edit·unclear → approve / reject → END |
| `transfer_execute` | `transfer_decision`, `transfer`, `owner_id` | 결과 메시지, 이체 State 전부 비움 | 항상 END |

**`transfer_interpret`의 규칙 (LLM이 분류하고 코드가 지킴)**
1. 응답이 정확히 "승인"/"거절"이면 **LLM을 거치지 않고** 즉시 처리한다.
2. 그 밖의 응답은 LLM이 `approve`/`reject`/`edit`/`unclear`로 분류한다.
3. **분류가 실패하거나(LLM 오류·None) `unclear`이면 절대 승인하지 않고** 이유를 안내한 뒤 다시 묻는다.
4. **승인이라고 분류돼도 수정 값이 함께 있으면 코드가 수정으로 강제 변경**한다. 수정은 승인을 겸하지 않으며, 바뀐 전체 화면을 보여주고 새로 승인받는다.
5. `edit`이 값 없이 오거나 현재와 같으면 다시 묻는다.
6. 수정이 유효하면 병합해 새 안을 보이고, 검증에 실패하면(잔액 초과·남의 계좌·같은 계좌·0원 이하) **이전 안을 유지**하고 이유를 안내한 뒤 승인 화면으로 돌아간다. 그래서 **승인 화면에 오는 이체안은 항상 검증을 통과한 안**이다.

**`transfer_execute`의 규칙**
- `transfer_decision`이 `approve`가 아니면 실행을 거부한다.
- 변경 직전에 **다시 검증**한다(승인을 기다리는 사이 잔액이 바뀔 수 있고, 검증 노드는 재개 때 다시 돌지 않는다).
- `apply_transfer`가 원본을 바꾸지 않고 잔액 2곳·거래 2건·처리 기록이 모두 반영된 **새 데이터**를 만들고, `save_data`가 **한 번에** 저장한다.

**`interrupt` 재개 동작 (설계의 전제, 실험으로 확인)**: 재개되면 멈췄던 노드(`transfer_approve`)가 **처음부터 다시 실행**되고 `interrupt()`는 이번에는 멈추지 않고 사용자의 응답을 반환한다. 이미 끝난 노드(`extract`, `validate`)는 다시 실행되지 않는다. 그래서 `interrupt` 앞에는 읽기 전용 코드(화면 계산)만 두고 변경은 뒤(다른 노드)에 둔다.

### 2.4 unsupported

카드 정지·재발급, 청구서, 계좌 별명 변경, 일상 대화 등은 코드가 정한 고정 문장으로 안내한다("계좌·잔액 조회, 거래내역 조회, 내 계좌 간 이체만 지원").

## 3. State 필드와 변경 시점

| 필드 | 값 | 채워지는 시점 | 비워지는 시점 |
|---|---|---|---|
| `messages` | 대화 이력(`add_messages`로 누적) | 매 턴 | 없음 |
| `owner_id` | 로그인한 사용자 | 첫 `invoke`에서 `main.py`가 한 번 넘김 | 바뀌지 않음 |
| `next` | supervisor의 라우팅 결과 | `supervisor` | 요청마다 덮어씀 |
| `transfer` | `TransferAccounts`(`from_account`, `to_account`, `amount`(`gt=0`), `date`) | `transfer_extract`(수정이 유효하면 `transfer_interpret`가 갱신, `date`는 `transfer_execute`가 채움) | 이체 흐름이 **어떤 경로로든 끝날 때** |
| `transfer_response` | 승인 화면에 대한 응답 원문 | `transfer_approve` | `transfer_interpret`가 읽은 직후, 종료 시 |
| `transfer_decision` | 분류 결과(`approve`/`edit`/`unclear`) | `transfer_interpret` | 다음 `transfer_approve` 진입 시, 종료 시 |
| `transfer_notice` | 다음 승인 화면 맨 위에 보일 안내 | `transfer_interpret` | 화면에 보여준 뒤, 종료 시 |

`transfer`는 잠깐 쓰는 작업대이지 기록이 아니다. 처리 이력은 `data.json`의 `requests`에 남긴다. LLM이 채우는 필드는 `from_account`, `to_account`, `amount`뿐이고 `owner_id`·별명·`date`는 코드가 정한다.

## 4. 분기 조건

| 상황 | 조건 | 결과 |
|---|---|---|
| 조회 | 본인 데이터만 대상 | 결과 나열(계좌 미지목이면 전체) |
| 조회 조건 오류 | 시작>종료, 음수·역전된 금액, `period`와 날짜 동시 지정, 날짜 형식 오류 | `{"error": ...}` 반환 → LLM이 안내 |
| 조회 0건 | 조건은 정상이나 거래 없음 | 적용된 조건과 함께 "내역 없음" |
| 남의 계좌·없는 계좌 | 소유자가 다름 | 동일한 메시지(존재 여부 비노출) |
| 지원하지 않는 요청 | supervisor가 `unsupported`로 분류 | 고정 안내문 |
| 이체 정보 부족 | 출금·입금·금액 중 하나라도 없음 | 부족한 항목과 보유 계좌를 안내하고 종료(다음 답변은 supervisor가 다시 `transfer_agent`로) |
| 이체 금액 오류 | 0 이하 | `TransferAccounts` 생성 시 걸려 되묻고 종료 |
| 이체 검증 실패 | 남의 계좌·없는 계좌, 같은 계좌, 잔액 부족 | 이유 안내, **승인 요청 없이** 종료 |
| 승인 | "승인" 또는 자연어 승인 | 재검증 → 변경·저장 → 결과 안내 |
| 거절 | "거절" 또는 자연어 취소·부정문 | 변경 없이 종료 |
| 승인 전 수정 | `edit` | 병합·검증, 유효하면 바뀐 전체 화면으로 새 승인 |
| 수정 후 검증 실패 | 병합한 안이 검증에 걸림 | 이전 안 유지 + 이유 + 승인 화면 복귀 |
| 승인+수정 혼합 | 분류가 approve여도 수정 값이 있음 | 수정으로 강제 변경, 새로 승인받기 전 실행 안 함 |
| 애매함·분류 실패 | `unclear`, LLM 오류, 값 없는 `edit`, 현재와 동일 | 승인하지 않고 이유 안내 후 다시 묻기 |
| 승인 대기 중 잔액 변경 | 실행 직전 재검증 실패 | 이유 안내, 데이터 불변 |
| 저장 실패 | 파일 저장 중 오류 | "반영되지 않았습니다", 데이터 불변 |
| 승인 확인 없는 실행 | `transfer_decision != approve` | `transfer_execute`가 거부 |

## 5. 데이터와 저장

- 원본 `data/initial_data.json`은 수정하지 않고, 작업용 복사본 `data/data.json`만 읽고 쓴다. `data_store.reset_data()`가 원본을 다시 복사한다.
- **모든 읽기·쓰기는 `data_store`를 거친다.** 저장은 같은 폴더의 임시 파일에 먼저 쓴 뒤 `os.replace`로 교체하므로 쓰는 도중 실패해도 기존 파일이 유지되고, 잔액 2곳·거래 2건·처리 기록이 전부 반영되거나 전부 반영되지 않는다.
- 이체가 만드는 데이터: 거래 2건(`withdrawal`/`deposit`, `card_id`·`merchant`는 `null`, 처리 기록과 연결하는 `request_id` 필드 추가)과 `requests` 기록 1건(`status="completed"`). 거래 시각은 기준일 + 실행 시점의 현재 시각(KST).
- 테스트는 `data_store.DATA_PATH`를 임시 복사본으로 바꿔서 실제 데이터를 건드리지 않는다.
- checkpointer가 `TransferAccounts`(pydantic)를 저장·복원할 때 "등록되지 않은 타입은 앞으로 차단될 예정"이라는 경고가 나서 `JsonPlusSerializer(allowed_msgpack_modules=...)`로 허용을 명시했다.

---

## 6. 초안에서 바뀐 내용과 이유

### 6.1 잔액 조회

| 초안 | 최종 | 이유 |
|---|---|---|
| 대화 중에 사용자 본인을 **인증**하는 단계 | 인증 단계 없음. 로그인했다고 가정하고 `owner_id`를 시작 시 State로 넘김 | 강의에서 "로그인했다고 치라"고 했고, "내 잔액" 요청에는 검증할 대상이 없다 |
| LLM이 `initial_data.json`을 직접 확인 | LLM은 **tool을 호출**하고 tool이 작업용 `data.json`을 읽음 | 원본은 보존, LLM이 원본 데이터에 직접 접근하지 않음 |
| `bank_account_balance`라는 좁은 subagent | 계좌 도메인 전체를 맡는 `account_agent` | 기능이 늘 때마다 subagent가 늘거나 합쳐야 하는 리팩터링을 피함 |
| `create_agent()` 기반 Agent-as-Tool(각 agent가 독립된 State) | **직접 StateGraph**로 부모·서브그래프가 State를 공유 | LangGraph의 State·Node·Edge를 직접 익히는 것이 목적. 공유 State라 `owner_id`를 클로저로 캡처할 필요가 사라짐 |
| supervisor가 결과를 검토하고 이상하면 재작업 요청 | **제거**, 라우팅과 결과 전달만 | 잔액 조회는 결정론적 조회라 "이상"의 의미가 불분명 |
| 계좌 개수(0/1/2개+)로 분기하고 2개 이상이면 `interrupt`로 선택 | **제거**. `account_call_model ↔ tools` ReAct 루프로 LLM이 답을 조립 | 계좌를 지목하지 않은 요청은 애매한 것이 아니라 "계좌 조회" 기능 자체이고, "생활비, 여행 둘 다"처럼 명시적으로 여러 개를 요청할 수도 있다. 자연어 해석은 LLM의 몫이며, "추가 질문"은 `interrupt`가 아니라 대화 한 턴으로 충분하다. `interrupt`는 되돌리기 어려운 작업의 승인용 |
| tool이 dict 반환, `account_id`·`balance`만 | JSON 문자열 반환, `nickname` 포함 | `ToolMessage`로 변환될 때 파이썬 repr이 되면 파싱이 깨질 수 있고, 별명 매칭에 `nickname`이 필요 |
| `owner_id`를 프롬프트로만 알려줌 | **`account_enforce_owner` 노드**로 tool 호출의 `owner_id`를 강제 교체 | 프롬프트 방어는 LLM이 지시를 따라줘야 하는 방어일 뿐이다(인젝션 시험은 막혔지만 코드가 막은 것이 아님) |
| 조회에서 날짜 관련 기준 없음 | 프롬프트에 **기준일 주입** | 새 대화에서 "9월 30일"을 LLM이 2024년으로 짐작해 0건이 나오는 조용한 버그가 있었다 |

### 6.2 거래내역 조회

| 초안 | 최종 | 이유 |
|---|---|---|
| 모든 CSV를 join한 종합 테이블 | **`data.json`이 원본 하나**, tool 호출 때 `transactions`+`accounts`+`cards`(left join)만 메모리에서 join | `bills`·`addresses`는 거래와 ID로 이어지지 않아 행이 복제되고, CSV는 이체로 `data.json`이 바뀌면 낡은 사본이 된다 |
| LLM이 view를 보고 `account_id`를 알아냄 | 잔액 조회 tool을 먼저 호출해 계좌 목록을 받고 → 거래내역 tool 호출(2단계) | LLM은 view를 볼 수 없고 tool이 반환한 문자열만 본다 |
| view에 query하는 tool | **정해진 파라미터**로 조건 전달 | 자유 쿼리는 오류가 조용히 틀리고("5만 원 이상"이 `>`인지 `>=`인지), 날짜 해석이 LLM으로 넘어가고, 실행 위험이 있다 |
| 기준일이 실제 오늘 | **2026-09-20으로 고정** | 데이터의 마지막 거래가 9/20이라 실제 오늘로는 "이번 주"가 항상 0건 |
| "결제"의 매핑 없음 | docstring과 프롬프트에 "결제 = 카드로 지출한 거래, `card_only=True`" 명시 | LLM이 "5만 원 이상 결제"에서 `card_only`를 켜지 않아 카드가 아닌 계좌 출금까지 섞여 6건이 나왔다(정답 5건) |
| LLM이 건수·합계를 계산 | tool이 유형별 합계와 건수를 **코드로 계산**해 반환 | LLM의 암산은 보장되지 않고, 입금·출금이 모두 양수라 합치면 의미 없는 값이 된다 |

### 6.3 이체

| 초안 | 최종 | 이유 |
|---|---|---|
| LLM이 이체 tool을 호출하고 **tool 안에서** 검증·`interrupt`·변경 (A) | **노드 4개로 분리해 State로 연결 (B)**, LLM은 이체 "요청"을 만드는 데까지만 관여 | tool의 반환값은 State에 들어가지 않는다. 돈이 움직이는 순간을 LLM이 아니라 코드가 실행한다 |
| `account_agent`가 이체도 담당 | 별도 **`transfer_agent`** | 승인·재개 흐름이 있어 조회와 노드 구성이 다르다 |
| 흐름이 승인/거절에서 끝남(승인 뒤 변경 단계 누락) | **`transfer_execute` 노드** | 승인 뒤에 재검증·변경·저장이 필요 |
| 종료 경로 2개(실패·거절) | 종료 경로 6개 이상(정보 부족, 검증 실패, 거절, 재검증 실패, 저장 실패, 성공) | 각 경로에서 이체 State를 비워야 다음 요청에 섞이지 않는다 |
| 정보 부족 경우 없음 | 추출 노드가 부족한 항목을 되묻고 종료 | "이체해줘" 같은 요청 처리 |
| `TransferAccounts`에 `owner_id`, 별명, 필수 `date` | `owner_id`·별명 제거, `date`는 실행 시점에 코드가 채움, `amount`는 `Field(gt=0)` | LLM이 채우면 신뢰할 수 없거나(`owner_id`), 코드가 조회할 수 있는 파생 값이거나(별명), LLM이 알 수 없는 값(`date`) |
| `Field(gt=0)`를 검증 노드에서 다룸 | **객체 생성 시(추출 노드)** 걸리고 검증 노드는 재확인 | 잘못된 금액으로는 `TransferAccounts` 자체를 만들 수 없다 |
| 승인 화면에 `tool`/`args` | **별명·금액·이체 전후 잔액을 담은 코드 생성 문장** | 사용자가 `acc-002`를 읽지 않고도 확인해야 LLM이 계좌를 잘못 골랐을 때 사람이 잡아낼 수 있다 |
| 승인 시 재검증 없음 | **실행 노드가 변경 직전에 재검증** | 검증 노드는 재개 때 다시 돌지 않고, 승인 대기 중 잔액이 바뀔 수 있다 |
| 저장 방식 미정 | **임시 파일 → 교체(원자적 저장)** | 잔액 2곳·거래 2건이 일부만 반영되면 안 된다 |
| 승인·거절이 정해진 두 단어 | (아래 6.4) | |

### 6.4 승인 흐름 (승인 전 수정, 자연어 승인·취소)

| 초안 | 최종 | 이유 |
|---|---|---|
| `main.py`가 "승인"/"거절"만 받아 `'approve'`/`'reject'` 전달 | **사용자가 친 원문을 그대로 전달**하고 그래프가 해석 | 자연어 응답("응, 진행해", "아니, 5만 원만")을 처리해야 함 |
| 응답 해석 위치 미정 | `transfer_interpret` 노드 하나에서 **LLM이 분류, 규칙은 코드가 적용** | "LLM은 해석, 코드는 검증·변경" 방침. 승인은 마지막 관문이라 LLM이 틀려도 결과가 안전해야 한다 |
| 수정은 tool의 반환값(`amount` dict) | **구조화 출력**(`with_structured_output`)의 선택 필드를 병합 | 이체 흐름에는 tool이 없고 tool 반환값은 State에 들어가지 않는다 |
| 수정 후 검증 실패 시 흐름 종료 | **이전 안 유지 + 이유 안내 + 승인 화면 복귀** | 한 번 잘못 말했다고 처음부터 다시 하지 않게. 데이터는 실행 노드에서만 바뀌므로 안전성은 같다 |
| 수정과 승인이 섞이면? | **수정으로 강제 변경**, 새로 승인받아야 실행 | 수정은 승인을 겸하지 않는다(코드가 강제) |
| 애매하거나 분류 실패 | **승인하지 않음**(`unclear`) | 기본값은 "실행하지 않음" |
| 필드 없음 | `transfer_response`, `transfer_decision`, `transfer_notice` 추가 | 응답 원문·분류 결과·수정 실패 이유를 다음 노드에 전달할 통로 |
| 순환의 무한 반복 우려 | 횟수 제한 없음 | 순환의 매 바퀴가 `interrupt`(사람의 입력)를 거치므로 자동으로 폭주할 수 없다 |
| 정확한 "승인"도 LLM이 해석 | 정확히 "승인"/"거절"은 **LLM 없이 즉시 처리** | 가장 흔한 경우를 결정론적으로 만들고 LLM 호출을 줄임 |

### 6.5 라우팅 (구현 막바지에 발견해 고친 것)

| 초안 | 최종 | 이유 |
|---|---|---|
| supervisor가 "그 밖의 요청도 가장 가까운 agent로" 보냄(목적지 2개) | **`unsupported` 목적지 추가**, 고정 안내문 | "미납 청구서 납부해줘"가 `transfer_agent`로 가서 "이체를 진행하려면 출금 계좌… 정보가 더 필요합니다"라는 **엉뚱한 되묻기**가 나왔다 |
| (고치는 과정) "카드"라는 단어가 나오면 `unsupported` | 카드로 결제한 **거래내역**은 `account_agent`, 카드 자체의 정지·재발급·목록만 `unsupported` | 첫 수정에서 "내 카드로 결제한 내역 보여줘"가 지원하는 기능인데도 `unsupported`로 갔다. 프롬프트에 구분을 명시해 12개 문장을 3회씩 확인 |

---

## 7. 검증 요약

- **LLM 없이**: `test_transfer.py` 107개(검증 함수, 변경 함수, 저장 원자성, 승인·거절, 검증 실패 4종, 재검증, 저장 실패, 자연어 승인·취소, 수정, 수정 실패 4종, 승인+수정 혼합, 분류 실패, 값 없는 수정, 실행 방어 등), `test_transactions.py` 21개, `test_enforce_owner.py`. LLM 분류기를 함수로 주입할 수 있게 만들어 분류 뒤의 규칙을 결정론적으로 테스트한다. 모두 임시 데이터 복사본으로 실행하고 실행 전후로 실제 `data.json`이 바이트 단위로 같음을 비교했다.
- **실제 LLM**: 조회 요청 다수(데이터에서 직접 다시 계산해 대조), 이체·승인 흐름 10개 시나리오, 라우팅 12개 문장 × 3회, 그리고 저장소를 **처음부터 clone해 `.env`만 만들어** 테스트와 `main.py`가 되는지 확인했다.
- 테스트 도중 발견한 것: 서브그래프가 `interrupt`로 멈춰 있는 동안 서브그래프 안의 State는 부모의 `values`에 없다. 멈춘 상태를 보려면 `get_state(..., subgraphs=True)`로 서브그래프 스냅샷을 읽어야 한다.

## 8. 남은 한계

- 미구현 기능은 1장 참고. 요청하면 정해진 안내문으로 답한다.
- **재시작·장애 복구**: `InMemorySaver`는 프로그램을 끄면 사라진다. 영속 checkpointer가 필요하다.
- `requests`에는 완료된 이체만 남는다. 거절·실패 기록이 있어야 후속 결과 조회를 만들 수 있다.
- LLM의 라우팅·응답 해석은 프롬프트에 좌우된다. 위험한 결정(승인)은 코드가 규칙으로 막지만 라우팅 정확도까지 보장하지는 못한다.
- 조회에서 뒤집힌 기간은 LLM이 tool 호출 전에 바로잡아서 tool의 오류 검증까지 도달하지 않는다(읽기 전용 조회에서는 해석한 기간을 안내하므로 허용했지만, 돈이 움직이는 기능에서는 다르게 다뤄야 한다).
- 이체 거래 시각이 기준일에 현재 시각을 붙이는 방식이라 같은 날 기존 거래 사이에 끼어들 수 있다.
